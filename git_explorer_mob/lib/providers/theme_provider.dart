/*
  Theme provider implementation disabled.

  Theme state is now driven by `Prefs` (see `shared_preferences_provider.dart`) and
  the Settings UI updates `Prefs` directly. The original Riverpod-based
  theme provider is commented out to avoid accidental usage.
*/
